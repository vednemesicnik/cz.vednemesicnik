// Only accounts on this Workspace domain may sign in via the passwordless
// methods (magic link, OAuth). Used to restrict sign-in to internal users.
export const ALLOWED_EMAIL_DOMAIN = 'vednemesicnik.cz'
